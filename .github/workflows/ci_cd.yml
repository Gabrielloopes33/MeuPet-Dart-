name: 🚀 CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:
    inputs:
      deploy_environment:
        description: 'Deployment environment'
        required: true
        default: 'staging'
        type: choice
        options:
        - staging
        - production

env:
  FLUTTER_VERSION: '3.24.3'
  JAVA_VERSION: '17'

jobs:
  # Stage 1: Code Quality & Security
  quality-gate:
    name: 🔍 Quality Gate
    uses: ./.github/workflows/code_quality.yml
    secrets: inherit

  # Stage 2: Testing
  test-suite:
    name: 🧪 Test Suite
    uses: ./.github/workflows/tests.yml
    needs: quality-gate
    secrets: inherit

  # Stage 3: Build Matrix (Parallel)
  build-matrix:
    name: 🏗️ Build Matrix
    needs: test-suite
    strategy:
      matrix:
        platform: [android, web]
        include:
          - platform: android
            workflow: ./.github/workflows/android.yml
          - platform: web
            workflow: ./.github/workflows/web.yml
    uses: ${{ matrix.workflow }}
    secrets: inherit

  # iOS Build (separate due to macOS requirement)
  build-ios:
    name: 🍎 Build iOS
    needs: test-suite
    if: github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v')
    uses: ./.github/workflows/ios.yml
    secrets: inherit

  # Stage 4: Deployment Coordination
  deploy-coordination:
    name: 🚀 Deploy Coordination
    runs-on: ubuntu-latest
    needs: [build-matrix, build-ios]
    if: always() && (needs.build-matrix.result == 'success' || needs.build-ios.result == 'success')
    
    steps:
      - name: 📊 Deployment Status
        run: |
          echo "## 🚀 Deployment Status Summary" >> $GITHUB_STEP_SUMMARY
          echo "### Platform Build Results:" >> $GITHUB_STEP_SUMMARY
          echo "- **Android**: ${{ needs.build-matrix.outputs.android-status || '✅ Success' }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Web**: ${{ needs.build-matrix.outputs.web-status || '✅ Success' }}" >> $GITHUB_STEP_SUMMARY
          echo "- **iOS**: ${{ needs.build-ios.result == 'success' && '✅ Success' || '⏭️ Skipped' }}" >> $GITHUB_STEP_SUMMARY
          
          echo "### Deployment Targets:" >> $GITHUB_STEP_SUMMARY
          if [[ "${{ github.ref }}" == "refs/heads/main" ]]; then
            echo "- 🌐 **Web**: GitHub Pages (Live)" >> $GITHUB_STEP_SUMMARY
            echo "- 🤖 **Android**: Internal Testing Track" >> $GITHUB_STEP_SUMMARY
          elif [[ "${{ github.ref }}" == refs/tags/v* ]]; then
            echo "- 🌐 **Web**: Firebase Hosting (Production)" >> $GITHUB_STEP_SUMMARY
            echo "- 🤖 **Android**: Play Store Internal" >> $GITHUB_STEP_SUMMARY
            echo "- 🍎 **iOS**: TestFlight" >> $GITHUB_STEP_SUMMARY
          else
            echo "- 📦 **Artifacts Only**: No deployment for this branch" >> $GITHUB_STEP_SUMMARY
          fi

  # Stage 5: Notifications
  notifications:
    name: 📢 Notifications
    runs-on: ubuntu-latest
    needs: [quality-gate, test-suite, build-matrix, build-ios, deploy-coordination]
    if: always()
    
    steps:
      - name: 📊 Calculate pipeline status
        id: status
        run: |
          # Determine overall status
          quality_status="${{ needs.quality-gate.result }}"
          test_status="${{ needs.test-suite.result }}"
          build_status="${{ needs.build-matrix.result }}"
          ios_status="${{ needs.build-ios.result }}"
          deploy_status="${{ needs.deploy-coordination.result }}"
          
          if [[ "$quality_status" == "success" && "$test_status" == "success" && 
                ("$build_status" == "success" || "$build_status" == "skipped") ]]; then
            echo "pipeline_status=success" >> $GITHUB_OUTPUT
            echo "status_icon=✅" >> $GITHUB_OUTPUT
          else
            echo "pipeline_status=failure" >> $GITHUB_OUTPUT
            echo "status_icon=❌" >> $GITHUB_OUTPUT
          fi

      - name: 📢 Slack Notification
        if: github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v')
        uses: 8398a7/action-slack@v3
        continue-on-error: true
        with:
          status: ${{ steps.status.outputs.pipeline_status }}
          channel: '#ci-cd'
          webhook_url: ${{ secrets.SLACK_WEBHOOK }}
          fields: repo,message,commit,author,action,eventName,ref,workflow
          custom_payload: |
            {
              "attachments": [{
                "color": "${{ steps.status.outputs.pipeline_status == 'success' && 'good' || 'danger' }}",
                "title": "${{ steps.status.outputs.status_icon }} MeuPet CI/CD Pipeline",
                "fields": [
                  {
                    "title": "Repository",
                    "value": "${{ github.repository }}",
                    "short": true
                  },
                  {
                    "title": "Branch/Tag",
                    "value": "${{ github.ref_name }}",
                    "short": true
                  },
                  {
                    "title": "Status",
                    "value": "${{ steps.status.outputs.pipeline_status }}",
                    "short": true
                  },
                  {
                    "title": "Commit",
                    "value": "${{ github.sha }}",
                    "short": true
                  }
                ]
              }]
            }

      - name: 📧 Email Notification (Failures)
        if: failure() && (github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v'))
        uses: dawidd6/action-send-mail@v3
        continue-on-error: true
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.EMAIL_USERNAME }}
          password: ${{ secrets.EMAIL_PASSWORD }}
          subject: "❌ MeuPet CI/CD Pipeline Failed - ${{ github.ref_name }}"
          to: ${{ secrets.NOTIFICATION_EMAIL }}
          from: MeuPet CI/CD <noreply@meupet.com>
          body: |
            The CI/CD pipeline for MeuPet has failed.
            
            Repository: ${{ github.repository }}
            Branch/Tag: ${{ github.ref_name }}
            Commit: ${{ github.sha }}
            
            Please check the GitHub Actions logs for more details:
            ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}

      - name: 📊 Final Summary
        if: always()
        run: |
          echo "## 📊 CI/CD Pipeline Summary" >> $GITHUB_STEP_SUMMARY
          echo "**Overall Status**: ${{ steps.status.outputs.status_icon }} ${{ steps.status.outputs.pipeline_status }}" >> $GITHUB_STEP_SUMMARY
          echo "**Repository**: ${{ github.repository }}" >> $GITHUB_STEP_SUMMARY
          echo "**Branch/Tag**: ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
          echo "**Commit**: ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
          echo "**Triggered by**: ${{ github.actor }}" >> $GITHUB_STEP_SUMMARY
          echo "**Event**: ${{ github.event_name }}" >> $GITHUB_STEP_SUMMARY
          
          echo "### Stage Results:" >> $GITHUB_STEP_SUMMARY
          echo "- **Quality Gate**: ${{ needs.quality-gate.result }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Test Suite**: ${{ needs.test-suite.result }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Build Matrix**: ${{ needs.build-matrix.result }}" >> $GITHUB_STEP_SUMMARY
          echo "- **iOS Build**: ${{ needs.build-ios.result }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Deploy Coordination**: ${{ needs.deploy-coordination.result }}" >> $GITHUB_STEP_SUMMARY